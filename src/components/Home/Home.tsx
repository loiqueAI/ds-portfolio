import React from "react";
import Image from "next/image";
import Hero from "../Hero";

type Props = {};

const Home = (props: Props) => {
  return (
    <div className="">
      <Hero />
    </div>
  );
};

export default Home;
