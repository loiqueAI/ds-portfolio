import Skills from "@/components/Skills/Skills";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Skills />
    </div>
  );
};

export default page;
